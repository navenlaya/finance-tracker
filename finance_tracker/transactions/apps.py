from django.apps import AppConfig

# App configuration for the transactions app
class TransactionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'transactions'
